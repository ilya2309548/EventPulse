route:
  receiver: webhook
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h
receivers:
  - name: webhook
    webhook_configs:
      - url: http://ingest:8080/alertmanager
        send_resolved: true
